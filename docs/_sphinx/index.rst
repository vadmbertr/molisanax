Stub generator
==============

This file exists only because Sphinx requires a ``master_doc``. The purpose of
this Sphinx project is to run ``sphinx-autodoc2`` and emit MyST API stubs into
``docs/_api/``; the resulting HTML build (if any) is not used.
